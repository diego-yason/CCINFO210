package models;

public enum FilterConditions {
	STUDENT_ID,
	STUDENT_DEGREE,
	COURSE_CODE,
	COURSE_DEPARTMENT,
	COURSE_ACADEMIC,
	COURSE_UNITS,
	NA,
	
	
}
//WHERE	code = ?
//WHERE	department = ?
//WHERE	units = ?
//WHERE	academic = ?;